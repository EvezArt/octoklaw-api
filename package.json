{
  "name": "octoklaw-api",
  "version": "1.0.0",
  "private": true,
  "description": "OctoKlaw Intelligence API — web intelligence as a service",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Serverless functions — no build step needed'",
    "test": "node test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=20"
  }
}
